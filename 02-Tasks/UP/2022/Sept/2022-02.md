```c++
void mystery(const char* str)
{
    while (*str && *(str+1)) {
        std::cout << *str;
        str += 2;
    }
}
```

Да се посочи какво ще изведе на екрана обръщението: mystery("abcdef");

Oтговор: ace